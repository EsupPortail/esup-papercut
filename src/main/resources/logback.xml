<?xml version="1.0" encoding="UTF-8"?>

<configuration>

	<!-- default configuration from springboot -->
	<include resource="org/springframework/boot/logging/logback/base.xml"/>

	<appender name="FILE" class="ch.qos.logback.core.FileAppender">
	    <file>logs/esup-papercut.log.log</file>
	    <append>true</append>
	    <immediateFlush>true</immediateFlush>
	    <encoder>
	      <pattern>%-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
	    </encoder>
	</appender>

	
	<appender name="EMAIL" class="ch.qos.logback.classic.net.SMTPAppender">
		<smtpHost>smtp</smtpHost>
		<smtpPort>25</smtpPort>
		<to>esup-papercut@mon-univ.fr</to>
		<from>esup-papercut@mon-univ.fr</from>
		<subject>%-5level - esup-papercut %date{dd/MM/yyyy HH:mm:ss}
		</subject>
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>[%-5level] %date{dd/MM/yyyy HH:mm:ss} [%-20thread]
				%logger{36} - %msg%n</pattern>
		</layout>
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>ERROR</level>
		</filter>
	</appender>
	
	<logger name="org.esupportail.papercut" level="error" additivity="false">
        <appender-ref ref="EMAIL"/>
    </logger>
    
    <logger name="org.esupportail.papercut" level="info" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>

	<root level="warn">
		<appender-ref ref="FILE"/>
	</root>	

</configuration>
